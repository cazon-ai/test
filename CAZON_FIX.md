# 🤖 Cazon AI Fix Instructions

**Error ID:** `20776815-306d-45bb-9160-abd8881524e2`

## Error Details

**Message:** Error: API rate limit exceeded

**File:** `src/services/api.js`
**Line:** 67

## 🔧 Suggested Fix

Implement exponential backoff retry logic:

const retryWithBackoff = async (fn, maxRetries = 3) => {
  for (let i = 0; i < maxRetries; i++) {
    try {
      return await fn();
    } catch (error) {
      if (error.message.includes('rate limit') && i < maxRetries - 1) {
        await new Promise(resolve => setTimeout(resolve, Math.pow(2, i) * 1000));
        continue;
      }
      throw error;
    }
  }
};

Also consider implementing request caching to reduce API calls.

## 📋 Implementation Steps

1. Review the error message and stack trace below
2. Apply the suggested fix to the affected file
3. Test the changes locally
4. Remove this file (CAZON_FIX.md) before merging

## Stack Trace

```
Error: API rate limit exceeded
    at APIClient.request (src/services/api.js:67:11)
    at UserService.fetchProfile (src/services/user.js:23:20)
    at async ProfilePage.loadData (src/pages/Profile.js:34:5)```

---
*Generated by [Cazon Error Intelligence](https://cazon.dev)*
